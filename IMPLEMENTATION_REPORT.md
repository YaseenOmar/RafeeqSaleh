# تقرير تنفيذ الرفيق الصالح

## ما تم تنفيذه

- إعادة تنظيم تدريجية إلى Domain/Data/Presentation مع إبقاء الملفات والميزات القديمة دون حذف.
- غلاف تطبيق RTL وتنقل سفلي للرئيسية والأذكار والقرآن والمفضلة والإعدادات.
- ثيم Material 3 فاتح وداكن أو حسب النظام، وإعدادات أحجام الخط.
- شاشة رئيسية قابلة للتمرير وبطاقات للأذكار والقرآن والمسبحة وبطاقة تابع القراءة.
- ست فئات أذكار، عداد متبقٍ، مؤشر تقدم، حفظ محلي، إعادة ضبط، نسخ ومشاركة.
- قرآن محلي: 114 سورة، الاسم والرقم وعدد الآيات ومكان النزول والبحث والقراءة الكسولة والتنقل بين السور.
- حفظ آخر موضع قراءة تلقائيًا مع debounce وسجل آخر عشرة مواضع وإعادة ضبط مؤكدة.
- علامات مرجعية مرتبة حديثًا، منع تكرار، فتح مباشر، وحذف بالسحب أو الزر.
- مسبحة محفوظة بهدف اختياري ومخصص وتقدم واهتزاز قابل للتعطيل وإعادة ضبط مؤكدة.
- صفحة إعدادات وبيان خصوصية داخل صفحة حول التطبيق، واسم Android العربي.

## أهم الملفات المعدلة أو المنشأة

- `lib/main.dart`: بدء التشغيل والثيم وتهيئة التخزين.
- `lib/domain/app_models.dart`: نماذج الإعدادات والتقدم والعلامات.
- `lib/data/local_store.dart`: مصدر التخزين والمستودع ومعالجة البيانات التالفة.
- `lib/presentation/app_controller.dart`: حالة التطبيق وعمليات الحفظ.
- `lib/presentation/app_shell.dart`: التنقل وجميع الشاشات الجديدة.
- `lib/data/azkar_lists.dart`: فئات الاستيقاظ والمتنوعة.
- `test/app_controller_test.dart` و`test/quran_data_test.dart`.
- `pubspec.yaml` وAndroid manifest وREADME وتقارير المشروع.

## القرارات التقنية

- استُخدم `ChangeNotifier` بدل حزمة إدارة حالة كبيرة لأن التطبيق محلي ومتوسط الحالة.
- استُخدم `shared_preferences` مع JSON منظم ومفاتيح إصدار؛ يتيح ترحيل البنية مستقبلًا ويحافظ على بساطة النسخة الأولى.
- استُخدمت حزمة `quran 1.4.1` كمصدر نص مضمّن دون شبكة، مع اختبار كامل للهيكل وعدد الآيات، بدل إنشاء النص يدويًا.
- قارئ القرآن يستخدم `ListView.builder` ولا يبني إلا الآيات المرئية. حفظ التمرير مؤجل 650ms ويُؤكد عند إغلاق الصفحة.
- الإشعارات وحدة مؤجلة حتى لا تضيف صلاحيات وخدمات خلفية ومخاطر نشر قبل استقرار النواة.

## المشاكل التي أُصلحت

- إزالة مواقيت الصلاة الوهمية والنص الذي يعد بميزات غير موجودة من الشاشة الرئيسية الجديدة.
- إصلاح بدء التطبيق غير المتزامن، الشاشة غير القابلة للتمرير، وعدم حفظ العدادات.
- استبدال الاختبار الافتراضي غير الصحيح باختبارات منطقية فعلية.
- إصلاح تحذيرات analyzer المهمة واسم التطبيق العربي على Android.
- حماية قراءة البيانات المحلية التالفة أو المفقودة بقيم افتراضية.

## نتائج التحقق

- `dart format lib test`: ناجح.
- `flutter analyze`: ناجح، بلا أخطاء أو تحذيرات.
- `flutter test`: 6 اختبارات ناجحة.
- تحقق القرآن: 114 سورة، 6236 آية، ولا يوجد اسم سورة أو نص آية فارغ.
- `flutter build apk --release`: ناجح.
- الناتج: `build/app/outputs/flutter-apk/app-release.apk` بحجم 58.4MB.

## الميزات المؤجلة

- تذكيرات الصباح والمساء والإشعارات المحلية.
- مفضلة مستقلة للأذكار (الشاشة الحالية توحّد علامات القرآن؛ النسخ والمشاركة متاحان للأذكار).
- سجل جلسات تسبيح مفصل.
- استخراج كل النصوص إلى ملفات ARB وإضافة واجهة إنجليزية؛ التطبيق مهيأ حاليًا بواجهة عربية ثابتة وFlutter localization للنظام.
- مراجعة شرعية تحريرية شاملة للأذكار القديمة المختصرة ومصادرها.
- اختبارات أجهزة/Golden شاملة لمقاسات شاشات متعددة؛ تمت معالجة الواجهات بقوائم وتمرير ومكونات مرنة.

## خطوات يدوية قبل Google Play

1. إنشاء keystore للمالك خارج المستودع وربط Release signing وفق وثائق Flutter/Android، لأن الإعداد الحالي يبني Release بمفتاح التطوير الموجود مسبقًا.
2. بناء AAB الموقّع: `flutter build appbundle --release`.
3. استضافة سياسة الخصوصية على رابط عام وإضافته إلى صفحة المتجر (النص داخل التطبيق موجود).
4. مراجعة الأيقونة ومواد المتجر ولقطات الشاشة على أجهزة فعلية، وإجراء مراجعة شرعية للنصوص.
5. رفع رقم البناء قبل كل إصدار وإجراء اختبار Play Console المغلق.

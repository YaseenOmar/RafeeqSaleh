# تدقيق مشروع الرفيق الصالح

## التقنية المستخدمة

- Flutter وDart مع Material Design، والهدفان المهيآن حاليًا Android وiOS.
- الحزمة الحالية: `com.yaseen.rafeeq_saleh`، والإصدار `1.0.0+1`.
- الحزم الحالية تركز على الواجهة (`flutter_screenutil` و`flutter_svg`) والتاريخ العربي/الهجري (`intl` و`hijri`).
- لا توجد قبل التنفيذ مكتبة إدارة حالة أو حقن اعتمادات أو قاعدة بيانات. وبسبب حجم التطبيق، الأنسب هو `ChangeNotifier` مع مستودع تخزين محلي بسيط بدل إدخال إطار ثقيل.

## الهيكلة الحالية

- `lib/screen`: أربع شاشات مستقلة (الرئيسية، فئات الأذكار، قائمة الذكر، المسبحة).
- `lib/widget`: عناصر واجهة قابلة لإعادة الاستخدام جزئيًا.
- `lib/data`: قوائم أذكار مكتوبة داخل Dart.
- `lib/model`: نموذج ذكر واحد.
- `lib/styling`: ألوان ثابتة للوضع الداكن فقط.
- التنقل الحالي يعتمد على named routes و`MaterialPageRoute` معًا، ولا يوجد غلاف تنقل رئيسي.

## الميزات الموجودة

- واجهة عربية RTL وشاشة رئيسية أولية.
- أذكار الصباح والمساء والنوم وما بعد الصلاة، مع إنقاص عداد الذكر داخل الجلسة.
- عداد تسبيح وهدف ثابتان داخل الذاكرة.
- أصول مرئية وSplash وأيقونة مهيأة مبدئيًا.

## الميزات الناقصة

- قراءة القرآن وقائمة السور والبحث والتنقل والعلامات المرجعية وتقدم القراءة.
- التخزين المحلي لكل الحالات والإعدادات.
- فئات الاستيقاظ والأذكار المتنوعة والأدعية.
- النسخ والمشاركة والمفضلة وإعادة الضبط وحفظ تقدم الأذكار.
- تنقل سفلي، شاشة مفضلة، إعدادات، سياسة خصوصية، ووضع فاتح/داكن/النظام.
- هدف مخصص واهتزاز اختياري وتأكيد إعادة ضبط المسبحة.
- localization منظم، اختبارات صحيحة، وتوثيق نشر.
- الإشعارات (ستبقى وحدة مؤجلة للنسخة التالية لتجنب صلاحيات وتعقيد نشر غير ضروريين).

## المشاكل البرمجية الموجودة

- تهيئة تنسيق التاريخ تتم بعد `runApp` بدل قبلها.
- النصوص مكتوبة مباشرة داخل الواجهات، والثيم لون داكن ثابت.
- الشاشة الرئيسية غير قابلة للتمرير وقد تفيض على الشاشات الصغيرة، وتعرض مواقيت صلاة تجريبية ثابتة.
- عدادات الأذكار تُنشئ نماذج جديدة وتحذف العناصر نهائيًا دون حفظ أو إعادة ضبط سليمة.
- عداد التسبيح لا يُحفظ، ويزيد الإجمالي حتى عند بلوغ الهدف، وإعادة الضبط بلا تأكيد.
- استخدام APIs قديمة وتحذيرات تنسيق، وTODOs في إعدادات Android والواجهة.
- اختبار Flutter الافتراضي يبحث عن عداد وأيقونة غير موجودين.
- Android Release موقّع بمفتاح debug، واسم التطبيق في manifest إنجليزي تقني.
- نصوص الأذكار الحالية مختصرة في مواضع؛ ستُحفظ ولا تعتبر مصدرًا قرآنيًا.

## خطة التنفيذ

1. إنشاء طبقة نماذج ومستودع تخزين محلي ومتحكم تطبيق مركزي قابل للاختبار.
2. توحيد الثيم والاتجاه والتنقل في Shell بخمس وجهات.
3. تحديث الرئيسية والأذكار ثم إضافة القرآن المحلي وتقدم القراءة والعلامات المرجعية.
4. إكمال المسبحة والمفضلة والإعدادات والخصوصية والنسخ والمشاركة.
5. إضافة اختبارات وحدات وواجهات، ثم formatter/analyzer/test/build release.
6. توثيق القرارات والمصدر والنتائج والخطوات اليدوية للتوقيع وسياسة المتجر.

## الملفات المخطط تعديلها أو إنشاؤها

- تعديل: `pubspec.yaml`، `lib/main.dart`، إعدادات Android، `README.md`، والملفات القديمة التي يمكن إعادة استخدامها.
- إنشاء مجلدات منظمة تحت `lib/core` و`lib/data` و`lib/domain` و`lib/presentation`، وأصول القرآن المحلية بحسب المصدر الموثوق المختار.
- إنشاء اختبارات للمستودع والمتحكمات والمنطق الأساسي تحت `test/`.
- إنشاء `IMPLEMENTATION_REPORT.md` بعد التحقق النهائي.

## قرارات ونطاق النسخة الأولى

- لا تسجيل دخول، ولا شبكة، ولا تحليلات أو تتبع.
- بيانات المستخدم تبقى محليًا.
- لا طلب لصلاحية الإشعارات في هذه النسخة؛ تُفصل التذكيرات كامتداد لاحق.
- توقيع المتجر النهائي يحتاج keystore خاصًا بالمالك ولن يُنشأ أو يُضمّن داخل المستودع؛ يمكن التحقق من Release محليًا مع إعداد التطوير الحالي وتوثيق خطوة التوقيع الآمن.
